name: Build ExileForge Database

on:
  workflow_dispatch:
  schedule:
    - cron: "20 4 * * *"

permissions:
  contents: write

concurrency:
  group: exileforge-database
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: ExileForge herunterladen
        uses: actions/checkout@v4

      - name: Node.js einrichten
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: RePoE-Daten importieren
        run: node tools/import-repoe-database.mjs

      - name: App-Datenbank erzeugen
        run: node tools/build-app-data.mjs

      - name: Daten prüfen
        run: |
          test -f generated/repoe/manifest.json
          test -f generated/app-data/manifest.json
          test -f generated/app-data/bases.json
          test -f generated/app-data/mods.json
          test -f generated/app-data/index.json
          test -d generated/app-data/pools

          find generated/app-data -type f -size +99M -print -quit | \
            grep -q . && {
              echo "Mindestens eine erzeugte Datei ist größer als 99 MB."
              exit 1
            } || true

      - name: Daten speichern
        run: |
          git config user.name "ExileForge Data Bot"
          git config user.email "actions@users.noreply.github.com"

          git add -f generated/repoe generated/app-v2 generated/app-data

          if git diff --cached --quiet; then
            echo "Keine Änderungen gefunden."
            exit 0
          fi

          git commit -m "ExileForge-Datenbank aktualisieren"
          git push
